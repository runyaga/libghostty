[package]
name = "ptyx"
version = "0.0.1"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
libc = "0.2"
portable-pty = "0.9.0"

[target.'cfg(unix)'.dependencies]
nix = { version = "0.28", features = ["term"] }

[build-dependencies]
cc = "1"
